<Window x:Class="peselSpr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:peselSpr"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <Label Content="Podaj PESEL do sprawdzenia:"/>
            <TextBox x:Name="peselTextBox" MaxLength="11"/>
            <Button Content="Sprawdź" Margin="0, 10, 0, 0" x:Name="checkButton" Click="checkButton_Click"/>
        </StackPanel>
        <Label x:Name="resultLabel" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,15,0,0"/>
    </Grid>
</Window>
